# Copie para .streamlit/secrets.toml e preencha com valores reais.

[database]
path = "data/ariam_pu.db"

[weather]
latitude = -23.31028
longitude = -51.16278

[smtp]
host = "smtp.gmail.com"
port = 587
username = "seu_email@dominio.com"
password = "SUA_SENHA_DE_APP"
use_tls = true

[users.admin]
# Gere hash com src.auth_service.AuthService.hash_password(...)
password_hash = "pbkdf2_sha256$260000$SEU_SALT$SEU_HASH"
role = "admin"

[users.qualidade]
password_hash = "pbkdf2_sha256$260000$SEU_SALT$SEU_HASH"
role = "qualidade"

[users.operador]
password_hash = "pbkdf2_sha256$260000$SEU_SALT$SEU_HASH"
role = "operador"
